schema: spec-driven

context: |
  **Pragmatic SDD Rule:**
  - All changes must be explicitly planned with OpenSpec before writing code.
  - No ad-hoc implementation of major architectural features without a documented change.
  - This project uses a `pnpm` monorepo structure.
  - All code commits must follow conventional commits.
